export default defineAppConfig({
    ui: {
        pagination : {
            wrapper: "flex items-center mt-2 -space-x-px",
        },
        selectMenu : {
            wrapper: "relative w-full sm:w-1/4",
        }
    }
})